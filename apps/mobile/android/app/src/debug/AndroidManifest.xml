<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only manifest overlay. Its only job is to point the application at the
    cleartext allowlist next to it; see res/xml/network_security_config.xml for
    why that is needed and why it must not reach a release build.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
